# Real holdings — NEVER commit. Holds your actual positions.
holdings.csv

# Performance tracking — local, personal, accumulated over time. NEVER commit.
snapshots.csv
contributions.csv

# Price cache
.price_cache.json
.price_cache.json.tmp

# Streamlit secrets — passwords / API keys / holdings. NEVER commit.
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.venv/
venv/
